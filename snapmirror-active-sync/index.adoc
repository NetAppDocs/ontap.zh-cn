---
sidebar: sidebar 
permalink: snapmirror-active-sync/index.html 
keywords: SM-BC, SMBC, failover, terms, benefits, snapmirror active sync, TR-4878 
summary: 使用SnapMirror主动同步保护具有LUN的应用程序、使应用程序能够透明地进行故障转移、从而确保发生灾难时的业务连续性。 
---
= 了解ONTAP SnapMirror主动同步
:hardbreaks:
:allow-uri-read: 
:nofooter: 
:icons: font
:linkattrs: 
:imagesdir: ../media/


[role="lead"]
SnapMirror 主动同步（在 ONTAP 9.15.1 之前称为 SnapMirror Business Continuity）允许业务服务在完整站点故障时继续运行。该技术使应用程序能够无缝地故障切换到辅助副本，而无需手动干预或自定义脚本。

SnapMirror 主动同步（SM-as）旨在通过自动故障转移提供更精细、更低成本、更易于使用的应用程序级保护。使用 SnapMirror 主动同步，您可以在地理位置分散的站点之间同步复制应用程序的多个卷。您可以在主副本中断的情况下自动故障转移到辅助副本，从而为一级应用程序实现业务连续性。

一些国家针对金融机构的规定要求企业定期从其二级数据中心提供服务。  SnapMirror主动同步及其高可用性集群可实现这些数据中心切换，从而实现业务连续性。

从 ONTAP 9.9.1 开始，AFF 和 ASA 集群上支持 SnapMirror 主动同步。主集群和辅助集群必须具有相同的类型：AFF、ASA 或 ASA r2。SnapMirror 主动同步可保护具有 iSCSI 或 FCP LUN 或 NVMe 命名空间的应用程序。

SnapMirror 活动同步支持 SAN 工作负载的对称和非对称配置。ONTAP 9.9.1 及更高版本支持非对称配置。对称主动/主动支持已在 ONTAP 9.15.1 中引入。对称主动/主动配置允许受保护 LUN 的两个副本通过双向同步复制执行读写 I/O 操作，使每个 LUN 副本均能为本地 I/O 请求提供服务。

从 ONTAP 9.19.1 开始，SnapMirror active sync 支持在 AFF 集群（仅限 2 节点）或 AFX 集群（仅限 4 节点）上，在 SVM 级别使用 NFS (v3 和 v4) 以及 SMB (2.x 或更高版本) 的 NAS 工作负载，且仅在主端提供读写访问。无法使用 NAS 协议访问辅助 SVM 及其包含的卷，但您可以将辅助卷克隆到其他 SVM 中以提供对它们的访问。


NOTE: 从2024年7月开始、以前以PDF格式发布的技术报告中的内容已与ONTAP产品文档集成。ONTAP SnapMirror主动同步文档现在包含了_TR-4878：SnapMirror active sync_中的内容。



[[benefits]]
== 优势

SnapMirror主动同步具有以下优势：

* 为业务关键型应用程序提供持续可用性。
* 能够从主站点和二级站点交替托管关键应用程序。
* 使用一致性组简化应用程序管理、以实现依赖写入顺序的一致性。
* 能够测试每个应用程序的故障转移。
* 即时创建镜像克隆、而不影响应用程序可用性。
* 能够在同一ONTAP集群中部署受保护和非受保护的工作负载。
* SAN 配置的 LUN、NVMe 命名空间、NVMe 子系统或存储单元标识以及 NAS 配置的卷标识保持不变，因此应用程序将它们视为共享虚拟设备。
* 能够重复使用二级集群、并灵活地创建即时克隆以供应用程序使用、以用于开发测试、UAT或报告目的、而不会影响应用程序性能或可用性。


SnapMirror 主动同步使您能够保护 SAN 配置中的数据 LUN 或 NVMe 命名空间以及 NAS 配置中的卷，从而使应用程序能够在发生灾难时透明地进行故障切换，以实现业务连续性。有关详细信息，请参见 link:use-cases-concept.html["用例"]。



[[key-concepts]]
== 关键概念

SnapMirror 主动同步使用 SAN 配置中的一致性组和 NAS 配置中的 SVM，以确保数据得到复制。SnapMirror 主动同步使用 ONTAP Mediator，或者从 ONTAP 9.17.1 开始，使用 Cloud Mediator 进行自动故障转移，以确保在发生灾难时提供数据。在规划 SnapMirror 主动同步部署时，了解 SnapMirror 主动同步及其架构的基本概念非常重要。

.不对称和对称
在对称主动/主动 SAN 集群配置中，两个站点都可以访问用于主动 I/O 的本地存储。对称主动/主动针对集群应用程序进行了优化，包括 VMware vMSC、带有 SQL 的 Windows Failover Cluster 和 Oracle RAC。

在非对称主动/主动 SAN 配置中，辅助站点上的数据将代理到 LUN、命名空间或存储单元。

有关详细信息，请参见 xref:architecture-concept.html[SnapMirror主动同步架构]。

在 NAS 配置中，没有代理，辅助 SVM 处于休眠状态，但您可以将卷克隆到另一个 SVM 以提供对其的访问。

.一致性组（仅限 SAN）
对于AFF和ASA系统link:../consistency-groups/index.html["一致性组"]是FlexVol卷的集合，为必须保护以保证业务连续性的应用程序工作负载提供一致性保证。在ASA r2 系统中，一致性组是存储单元的集合。

一致性组的目的是同时对卷或存储单元集合进行快照，从而确保在某个时间点集合的崩溃一致性副本。一致性组确保数据集的所有卷都处于静止状态，然后在同一时间点进行快照。这为支持数据集的卷或存储单元提供了数据一致的还原点。因此，一致性组可以维护相关的写入顺序一致性。如果您决定保护应用程序以实现业务连续性，则必须将与此应用程序对应的卷或存储单元组添加到一致性组中，以便在源一致性组和目标一致性组之间建立数据保护关系。源一致性组和目标一致性组必须包含相同数量和类型的卷。

.SVM 和 FlexVol 支持（NAS 的 SnapMirror 主动同步）
在 ONTAP 9.19.1 中，适用于 NAS 的 SnapMirror active sync 支持对包含 FlexVol 卷的 SVM 进行保护。NAS 配置中的 SnapMirror active sync 不支持 SVM 内的一致性组或 FlexGroup 卷。

.成分卷
在 SAN 配置中，组成部分是单个卷、LUN 或 NVMe 命名空间（从 ONTAP 9.17.1 开始），是 SnapMirror 活动同步关系中受保护的一致性组的一部分。

在 NAS 配置中，组成部分是卷，包含的 SVM 是在 SnapMirror 活动同步关系中受保护的实体。

.ONTAP 调解器
这link:../mediator/index.html["ONTAP 调解器"]接收有关对等ONTAP集群和节点的运行状况信息，在两者之间进行协调，并确定每个节点/集群是否运行正常且正在运行。ONTAP调解ONTAP提供以下方面的运行状况信息：

* 对等ONTAP集群
* 对等ONTAP集群节点
* 故障转移单元（对应于 SAN 关系的 SnapMirror 活动同步中的一致性组和 NAS 关系的 SnapMirror 活动同步中的 SVM）；对于每个故障转移单元，提供以下信息：
+
** 复制状态：未初始化、同步或不同步
** 哪个集群托管主副本
** 操作上下文(用于计划内故障转移)




利用此ONTAP调解器运行状况信息、集群可以区分不同类型的故障、并确定是否执行自动故障转移。ONTAP调解器是SnapMirror活动同步仲裁以及两个ONTAP集群(主集群和二级集群)中的三方之一。要达成协商一致意见，法定人数中至少有两个当事方必须同意某项行动。


NOTE: 从ONTAP 9.15.1开始、System Manager将显示任一集群的SnapMirror活动同步关系的状态。您还可以从System Manager中的任一集群监控ONTAP调解器的状态。在早期版本的ONTAP中、System Manager会显示源集群中SnapMirror活动同步关系的状态。

.ONTAP云调解器
ONTAP Cloud Mediator 从ONTAP 9.17.1 开始可用。ONTAPONTAP Cloud Mediator 提供与ONTAP Mediator 相同的服务，只是它使用NetApp控制台托管在云中。

.计划内故障转移
用于更改SnapMirror活动同步关系中副本角色的手动操作。主站点将成为二级站点、而二级站点将成为主站点。

.自动计划外故障转移(AUTOFO)
对镜像副本执行故障转移的自动操作。此操作需要ONTAP调解器的帮助才能检测到主副本不可用。

.主要-优先和主要偏倚
SnapMirror主动同步采用主优先原则、在网络分区情况下、优先使用主副本来提供I/O。

主偏置是一种特殊的仲裁实施方式、可提高受SnapMirror活动同步保护的数据集的可用性。如果主副本可用、则当无法从两个集群访问ONTAP调解器时、主偏置将生效。

自 ONTAP 9.11.1 起，SnapMirror 活动同步 SAN 配置支持主优先和主偏置。主副本在 System Manager 中指定，并通过 REST API 和 CLI 输出。

.不同步(OOS)
如果应用程序I/O未复制到二级存储系统，则会报告为**不同步**。不同步状态表示二级卷未与主卷(源卷)同步、并且未进行SnapMirror复制。

如果镜像状态是 `Snapmirrored`，这表明SnapMirror关系已建立且数据传输已完成，这意味着目标卷与源卷保持同步。

SnapMirror主动同步支持自动重新同步、使副本能够返回到InSync状态。

从ONTAP 9.15.1开始、SnapMirror主动同步支持 link:interoperability-reference.html#fan-out-configurations["在扇出配置中自动重新配置"]。

.统一和非统一配置
* **统一主机访问**意味着两个站点的主机都连接到两个站点上存储集群的所有路径。跨站点路径跨距离延伸。统一主机访问仅适用于 SAN 配置。
* **非统一主机访问**意味着每个站点的主机仅连接到同一站点的集群。跨站点路径和延伸路径未连接。



NOTE: 任何SnapMirror主动同步部署均支持统一主机访问；只有对称主动/主动部署才支持非统一主机访问。

.零RPO
RPO表示恢复点目标、即在给定时间段内视为可接受的数据丢失量。零RPO表示不允许丢失任何数据。

.零RTO
RTO表示恢复时间目标、是指在发生中断、故障或其他数据丢失事件后、应用程序可以无中断地恢复正常运行的时间量。RTO为零表示任何停机时间都不可接受。



[[snapmirror-active-sync-configuration-support-by-ontap-version]]
== ONTAP版本支持的SnapMirror主动同步配置

SnapMirror 活动同步支持的集群、协议和配置类型因您的 ONTAP 版本而异。该表显示了添加更新的 ONTAP 版本以及支持这些更新的配置。

[cols="2*"]
|===
| ONTAP 版本 | 添加了对以下内容的支持： 


| 9.19.1 及更高版本  a| 
* NFS
* SMB


NAS 协议支持以下集群类型：

* AFF
* AFX




| 9.17.1 及更高版本  a| 
* 适用于 VMware 主机工作负载的 NVMe




| 9.16.1及更高版本  a| 
* ASA r2，双节点集群
* 对称主动/主动 4 节点到 4 节点 AFF 或 ASA 集群
+
对称主动/主动配置除支持 2 节点到 2 节点集群外，还支持 ONTAP 9.16.1 及更高版本中的 4 节点到 4 节点集群。

+
对于 ASA r2，仅支持 2 节点到 2 节点集群。





| 9.15.1 及更高版本  a| 
以下配置类型：

* 对称主动/主动
+
对称主动/主动配置支持 ONTAP 9.15.1 中的 2 节点集群。





| 9.9.1 及更高版本  a| 
* AFF
* ASA （仅限 C 系列）


协议:

* iSCSI
* FC


配置类型：

* 非对称主动/主动


|===
主集群和辅助集群必须是同一类型： link:../san-admin/learn-about-asa.html["ASA"] ， link:https://docs.netapp.com/us-en/asa-r2/get-started/learn-about.html["ASA r2"^]或AFF。

有关其他配置信息，请参见:

* link:prerequisites-reference.html["SnapMirror 活动同步先决条件"]
* link:limits-reference.html["SnapMirror 活动同步对象限制"]

